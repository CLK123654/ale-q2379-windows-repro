# Shared metering tenant access handover

This repository contains one PostgreSQL task and its independent Windows Server 2025 reproduction workflow.

The task package is under task. The implementation and QA directories contain the reproducible delivery builder and workflow checks. The public material uses synthetic tenant and metering data.
